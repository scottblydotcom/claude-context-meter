# Xcode user data
*.xcuserstate
xcuserdata/

# Build artifacts
DerivedData/
build/
*.o
*.d

# macOS
.DS_Store
.worktrees/

# AI agent config — holds private infra (tailnet fleet host, key-service names).
# Same rationale as the memory-dir rule. MUST NOT be committed to this public repo.
.claude/
session-notes/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
